{
  "name": "amecc-portal",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "dev": "wrangler dev",
    "deploy:worker": "wrangler deploy",
    "user:create": "node scripts/create_user.js",
    "db:migrate:local": "wrangler d1 migrations apply amecc-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply amecc-db --remote"
  },
  "dependencies": {
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}